{
  "name": "emberdeep",
  "version": "0.4.0",
  "private": true,
  "description": "A dependency-free single-player turn-based dungeon crawler.",
  "scripts": {"test": "node --test tests/*.test.cjs", "build": "node build.cjs", "start": "node server.cjs"}
}
